# Server Configuration
PORT=5000
NODE_ENV=development
FRONTEND_URL=http://localhost:3000

# Email Configuration (for contact forms and newsletters)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Database Configuration (if you decide to add a database later)
# DATABASE_URL=postgresql://username:password@localhost:5432/aurora_luxe

# JWT Secret (for authentication if needed)
# JWT_SECRET=your-super-secret-jwt-key

# API Keys (for external services)
# GOOGLE_MAPS_API_KEY=your-google-maps-api-key
# STRIPE_SECRET_KEY=your-stripe-secret-key
